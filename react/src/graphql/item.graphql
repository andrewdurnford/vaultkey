query Item($id: ID!) {
  item(id: $id) {
    id
    name
    username
    password
  }
}

query Items {
  items {
    id
    name
    username
  }
}

mutation ItemCreate($input: ItemCreateInput!) {
  itemCreate(input: $input) {
    id
    name
    username
    password
  }
}
